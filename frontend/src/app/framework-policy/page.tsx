import StaticPdfSectionPage from "@/components/StaticPdfSectionPage";

export default function FrameworkPolicyPage() {
  return <StaticPdfSectionPage title="Framework & Policy Information" description="Maharashtra CSR framework, policy guidance, Schedule VII alignment and convergence workflow." items={["CSR framework overview", "Schedule VII guidance", "Government-corporate convergence model", "Policy notes and circular references"]} />;
}
